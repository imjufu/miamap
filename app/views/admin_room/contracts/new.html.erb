<div class="d-flex align-items-center mb-3">
  <h1 class="h1 mr-2"><%= t('contracts.new') %></h1>
  <%= link_to t('actions.cancel'), admin_room_producer_path(@producer), class: 'btn btn-light' %>
</div>

<%= render 'form', producer: @producer, contract: @contract %>
