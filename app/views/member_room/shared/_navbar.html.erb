<nav class="navbar navbar-expand-md navbar-dark bg-primary fixed-top">
  <%= link_to safe_join([t('project.name_html'), ' / ', t('project.member')]), member_room_root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbars" aria-controls="navbars" aria-expanded="false" aria-label="<%= t('actions.toggle_menu') %>">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="navbar-collapse collapse" id="navbars">
    <% if member_signed_in? %>
    <ul class="navbar-nav mr-auto">
    </ul>
    <ul class="navbar-nav">
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="memberMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= t('project.welcome_html', user: current_member.first_name) %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="memberMenuLink">
          <%= link_to(t('devise.shared.links.sign_out_html'), destroy_session_path(current_member), method: :delete, class: 'dropdown-item') %>
        </div>
      </li>
    </ul>
    <% else %>
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= link_to t('navbar.back_to_the_root_html'), root_path, class: 'nav-link' %>
      </li>
    </ul>
    <% end %>
  </div>
</nav>
