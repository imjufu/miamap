<div class="text-center card-deck">
  <div class="card">
    <div class="card-header"><%= t('welcome.new_members.title') %></div>
    <div class="card-body">
      <h5 class="card-title"><%= t('welcome.new_members.subtitle') %></h5>
      <p class="card-text"><%= t('welcome.new_members.description_html') %></p>
      <%= link_to t('welcome.new_members.go'), members_sign_up_step_1_path, class: 'btn btn-primary btn-lg' %>
    </div>
  </div>
  <div class="card">
    <div class="card-header"><%= t('welcome.old_members.title') %></div>
    <div class="card-body">
      <h5 class="card-title"><%= t('welcome.old_members.subtitle') %></h5>
      <p class="card-text"><%= t('welcome.old_members.description_html') %></p>
      <%= link_to t('welcome.old_members.go'), new_member_session_path, class: 'btn btn-primary btn-lg' %>
    </div>
  </div>
</div>
