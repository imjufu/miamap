<div class="d-flex align-items-center mb-3">
  <h1 class="h1 mr-2"><%= t('membership_forms.new') %></h1>
  <%= link_to t('actions.cancel'), membership_forms_path, class: 'btn btn-light' %>
</div>

<%= render 'form', membership_form: @membership_form %>
