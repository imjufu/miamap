<div class="d-flex align-items-center mb-3">
  <h1 class="h1 mr-2"><%= @user.full_name %></h1>
  <%= link_to t('actions.cancel'), admin_room_user_path(@user), class: 'btn btn-light' %>
</div>

<%= render 'form', user: @user %>
